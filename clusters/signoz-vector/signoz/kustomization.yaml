apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: signoz-vector

resources:
- helm/helmrepo.yaml
- helm/helmrelease.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- name: signoz-values
  files:
  - values.yaml=values/signoz.yaml
